// frontend/js/notifications.js

const Notifications = {
  async load() {
    // Hozircha — backendda alohida "notifications" endpoint yo'q.
    // Shu sababli bu funksiya kelajakda kengaytiriladi.
    // Hozircha bo'sh massiv qaytaramiz.
    return [];
  }
};
window.Notifications = Notifications;